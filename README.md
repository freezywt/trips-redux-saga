# Trips

### Technologies used

| `Axios` | `Redux` | `Saga` | `React Router`  |

## How to use
 #### install dependencies :
 `npm install`
 #### run project :
 `npm start`
 #### build project :
 `npm build`
